// Legacy duplicado: mantener placeholder hasta poder eliminar/renombrar físicamente.
import { describe, it, expect } from "vitest";

describe("legacy: principal.test.ts (disabled)", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
