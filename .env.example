# Copia este archivo a .env.development o .env.local y completa los valores reales

# Variables estilo Vite (compatibilidad con herramientas de test/build)
VITE_COGNITO_REGION=us-east-1
VITE_USER_POOL_ID=us-east-1_XXXXXXXXX
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Equivalentes expuestas en Next.js para uso en cliente
NEXT_PUBLIC_COGNITO_REGION=${VITE_COGNITO_REGION}
NEXT_PUBLIC_COGNITO_USER_POOL_ID=${VITE_USER_POOL_ID}
NEXT_PUBLIC_COGNITO_CLIENT_ID=${VITE_COGNITO_CLIENT_ID}
